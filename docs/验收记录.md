# 实现与验收记录

日期：2026-09-12。状态：本地样品实现与程序验证完成；真实模型验收和外部试用未完成。
环境：macOS arm64、Python 3.10.21、Node 24.18.0。

## 已实现

- 独立 React/TypeScript 前端、FastAPI API、SQLite 私有数据与 Node Satori/resvg 渲染。
- 邀请口令、独立浏览器身份、所有权校验、同源写请求限制、失败回传与每日额度。
- 单 Agent 循环、严格参数/权限检查、8 轮上限、有限重试、数字轨迹、持久任务和重启恢复。
- 作品库、不可变版本、失败重试、关联数据删除；预览/下载使用同一实际 PNG。
- 三种卡片模板、反馈改稿、直接改文案、显式偏好保存/清除；改稿可基于选中的历史版本。
- 初始化、启动、模型检查、指标导出脚本；Dockerfile 和 Compose。

## 自动验证

- 后端：**40 passed，6 skipped**。跳过项属于另一项目类型的专属用例，不计入通过数。
- 渲染：**8/8 passed**。三模板中文、最大字数、实际图片与长地点/旁白边界、禁止外部图片 URL。
- TypeScript 类型检查和 Vite 生产构建通过。
- 每项目 **20/20 人工构造场景可导出**。调用真实渲染，但模型为注入替身；不能据此声称真实模型完成率 ≥90%。
- 覆盖无配置、空/超长/非法参数、未知工具、偏好权限、跨用户/作品、CSRF、额度事务回滚、超时重试、最大轮次、渲染失败后恢复、重启与幂等、删除清理。
- 日志白名单不保存票面、提示词、参数或密钥，测试验证了过滤。
- 有一条 Starlette/AnyIO 弃用提示，不影响通过；后续随依赖更新处理。
- 详细输出见 backend-tests.txt 和 build.txt。

## 浏览器验证

检查了默认桌面与 390×844 手机尺寸，没有横向溢出，示例加载正常，未配置模型明确不可用。交互检查在 `/tmp` 独立数据目录中使用人工作品，未写入正式作品库。

已检查：风格切换、邀请登录、显式偏好保存、作品库、直接改副标题和保存第二版。

自动文件选择被 Chrome 扩展的文件 URL 访问权限限制，未完成浏览器端自动上传；API 测试已验证上传、内容校验、去重、数量限制。不能把 API 覆盖记成浏览器上传通过。

## 实际能力检查

三种风格的静态中文字体与排版示例经过实际渲染和目视检查，修复了装饰遮挡文字和页脚重叠，加入文字边界检查。

## 尚未验收

1. MODEL_BASE_URL、MODEL_ID、MODEL_API_KEY 未配置；真实连接检查明确返回“模型未配置”。未读取或复制蓝图凭据，未产生云模型调用。
2. 配置后运行 scripts/check_model.py，再按样例清单.json 完成各 20 组真实模型闭环；检验局部改稿、文案质量、事实依据与补问体验。
3. 5–10 人试用、真实完成率 ≥90%、再使用意愿尚无数据；trial.csv 已准备。
4. 本机未安装 Docker，镜像构建、HTTPS 反向代理和外网发布未执行。
5. 首版按单次活动建模，事件字段保存在素材确认元数据与版本 facts；多活动档案后续独立建事件表。
6. 会话不能跨浏览器恢复，配额不能防止重新注册；OCR 脱敏为规则方案。年度档案、地图、视频和自动抠图按计划暂缓。

本阶段可评审实现与模板体验，尚不能标记为完成真实模型内测验收或已上线。
