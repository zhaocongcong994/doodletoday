# Run setup.py to generate project-specific local invitation and renderer credentials.
INVITE_CODE=
RENDER_TOKEN=
# OpenAI-compatible Chat Completions endpoint, ending in /v1 (or provider equivalent).
MODEL_BASE_URL=
MODEL_API_KEY=
MODEL_ID=
MODEL_TIMEOUT=45
MAX_ROUNDS=8
MAX_RETRIES=2
DAILY_LIMIT=20
SECURE_COOKIE=false
OCR_ENABLED=false
OCR_TIMEOUT=180
# Optional: DATA_DIR, RENDER_URL, RENDER_PORT, RENDER_HOST, API_HOST
